// คลิปปักหมุดบนหน้าแรก
// ---------------------------------------------------------------------------
// ตั้งไว้ตรงนี้ ไม่ได้อยู่ในฐานข้อมูล เพราะเป็นการตัดสินใจของบรรณาธิการว่าจะให้
// อะไรอยู่หัวแถบ ไม่ใช่ข้อมูลที่ระบบดึงเข้ามาเอง เปลี่ยนคลิปคือแก้ไฟล์นี้แล้ว
// deploy ซึ่งเห็นใน git ว่าใครเปลี่ยนอะไรตอนไหน
//
// ไฟล์อยู่ใน public/ จึงถูกส่งจากขอบเครือข่ายเหมือนรูปอื่น ไม่ผ่าน Worker และ
// ไม่กินโควตาคำขอ ข้อแลกคือมันอยู่ในรีโป — คลิปที่ปักหมุดต้องย่อให้เล็กก่อน
// (Cloudflare จำกัดไฟล์ละ 25 MB) ตัวนี้ย่อจาก 4K 113 MB เหลือ 1080×1920 12 MB
//
// ตั้งเป็น null เมื่อไม่อยากปักหมุดอะไร แถบจะหายไปทั้งแถบ ไม่ทิ้งช่องว่างไว้

export type FeaturedClip = {
  src: string;
  poster: string;
  title: string;
  description: string;
  /** วินาที — ใช้บอกผู้อ่านล่วงหน้าว่าคลิปยาวแค่ไหน */
  durationSec: number;
  /** ป้ายมุมบนซ้าย บอกว่าทำไมคลิปนี้ถึงได้อยู่ตรงนี้ */
  badge: string;
};

export const FEATURED_CLIP: FeaturedClip | null = {
  src: "/featured/gog-featured.mp4",
  poster: "/featured/gog-featured.jpg",
  title: "GOG ลงพื้นที่ — ภาพจากขอบสนาม",
  description:
    "คลิปแนวตั้งจากทีมงาน GOG ถ่ายเอง ตัดเอง ไม่ได้หยิบมาจากช่องไหน",
  durationSec: 46,
  badge: "ปักหมุด",
};
